# Local / demo profile: adds the fixture migrations on top of the schema.
spring:
  flyway:
    locations: classpath:db/migration, classpath:db/seed

logging:
  level:
    com.xtramile.patient: DEBUG
    org.hibernate.SQL: DEBUG

# Keep the build context small and the image reproducible: everything the build
# needs is the POM and src/, and anything else risks leaking host state into it.
target/
.mvn/
mvnw
mvnw.cmd
Dockerfile
.dockerignore
.gitignore
.gitattributes
*.md
